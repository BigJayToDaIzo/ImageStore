---
import Layout from '../layouts/Layout.astro';
import AppContainer from '../components/AppContainer.svelte';
---

<Layout title="ImageStore">
	<div class="app">
		<header class="header">
			<h1 class="logo"><img src="/favicon.svg" alt="" class="logo-icon" />ImageStore</h1>
		</header>

		<main class="main">
			<AppContainer client:only="svelte" />
		</main>
	</div>
</Layout>

<style>
	.app {
		display: flex;
		flex-direction: column;
		height: 100vh;
	}

	.header {
		padding: 0.25rem 0.5rem;
		background: #fff;
		border-bottom: 1px solid #ddd;
	}

	.logo {
		display: flex;
		align-items: center;
		gap: 0.375rem;
		font-size: 1.5rem;
		font-weight: 600;
		margin: 0;
		color: #2563eb;
	}

	.logo-icon {
		width: 2rem;
		height: 2rem;
	}

	.main {
		flex: 1;
		display: flex;
		overflow: hidden;
	}
</style>
