---
import Layout from '../layouts/Layout.astro';
import AppContainer from '../components/AppContainer.svelte';
---

<Layout title="ImageStore">
	<div class="app">
		<header class="header">
			<h1 class="logo">ImageStore</h1>
		</header>

		<main class="main">
			<AppContainer client:only="svelte" />
		</main>
	</div>
</Layout>

<style>
	.app {
		display: flex;
		flex-direction: column;
		height: 100vh;
	}

	.header {
		padding: 0.75rem 1.5rem;
		background: #fff;
		border-bottom: 1px solid #ddd;
	}

	.logo {
		font-size: 1.25rem;
		font-weight: 600;
		margin: 0;
		color: #2563eb;
	}

	.main {
		flex: 1;
		display: flex;
		overflow: hidden;
	}
</style>
